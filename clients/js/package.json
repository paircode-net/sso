{
  "name": "@sso/client",
  "version": "0.1.0",
  "description": "JWT claim helpers for products consuming the SSO Authorization Server",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test dist/index.test.js"
  },
  "files": [
    "dist",
    "src"
  ],
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
